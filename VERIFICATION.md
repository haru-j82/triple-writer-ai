# 코드 변경 검증 및 검토 체크리스트

## 원칙
모든 코드 변경 후에는 반드시 이 체크리스트를 따라 검증하고 결과를 최종 보고할 때 포함합니다.

---

## 1단계: 코드 변경 검증

### 1.1 문법 및 참조 검사
- [ ] 모든 변수명이 올바른지 확인 (오타, 대소문자)
- [ ] 모든 import/export가 정확한지 확인
- [ ] 배열/객체 메서드가 올바른지 확인 (예: `.random` 같은 잘못된 속성 확인)
- [ ] 함수 매개변수 개수와 타입이 맞는지 확인

### 1.2 전체 참조 검사
```bash
grep -r "변경된_변수명" --include="*.tsx" --include="*.ts" 
```
- [ ] 모든 참조가 새 이름으로 변경되었는지 확인
- [ ] 이전 이름의 잔여 참조가 없는지 확인

### 1.3 로직 흐름 검증
- [ ] 변경 전후 로직 흐름이 동일한지 확인
- [ ] 부작용(side effects)이 없는지 확인
- [ ] 조건문이 올바른지 확인

---

## 2단계: 기능별 테스트

### 2.1 이미지 생성 흐름
- [ ] 첫 번째 이미지(썸네일) 생성 성공
- [ ] 같은 프롬프트로 재생성 시 다른 이미지 반환
- [ ] 문단별 이미지 생성 시 각각 다른 이미지 반환
- [ ] 새로 고침 후 이전 이미지 삭제됨

### 2.2 크레딧 시스템
- [ ] 이미지 생성 시 크레딧 차감
- [ ] 크레딧 한계 시 생성 불가
- [ ] 대시보드에 크레딧 표시됨

### 2.3 UI/UX
- [ ] 콘솔에 에러가 없음
- [ ] 모든 버튼이 정상 작동
- [ ] 로딩 상태가 올바르게 표시됨

---

## 3단계: 최종 보고

각 수정사항마다 다음을 포함하여 보고:
1. **문제**: 무엇이 잘못됐는가
2. **원인**: 왜 발생했는가
3. **해결책**: 어떻게 고쳤는가
4. **검증**: 어떻게 확인했는가
5. **영향범위**: 어떤 파일/기능에 영향을 주는가

---

## 최근 수정사항 검증 (2026-08-10)

### 수정 1: setSelectedPosition 에러
**문제**: ReferenceError: setSelectedPosition is not defined
**원인**: State 변수명을 selectedPositions로 변경했으나 일부 참조 누락
**해결**: 136번 줄 handleGenerate에서 `setSelectedPositions(new Set())`로 수정
**검증**: `grep setSelectedPosition` 실행 → 모두 setSelectedPositions로 변경됨 확인
**영향범위**: app/agent/images/page.tsx (이미지 생성 모달)

### 수정 2: API 호출 순서 변경
**문제**: Pixabay만 사용하여 이미지 다양성 부족
**원인**: API 호출 순서가 최적화되지 않음
**해결**: 우선순위 변경 (Unsplash > Pixabay > Pexels)
**검증**: lib/imageApi.ts 55-70번 줄 확인
**영향범위**: lib/imageApi.ts fetchRealImage 함수

### 수정 3: Unsplash Source API 추가
**문제**: Unsplash `/photos/random` 엔드포인트는 Client ID 필요
**원인**: 인증 없는 방법을 사용하지 않음
**해결**: `https://source.unsplash.com/` API 사용 (리다이렉트 기반)
**검증**: 312-330번 줄 fetchFromUnsplash 함수 검토
**영향범위**: lib/imageApi.ts

### 수정 4: Pexels 다양성 개선
**문제**: 항상 같은 페이지의 첫 번째 이미지 반환
**원인**: 페이지 1, 결과 첫 번째만 사용
**해결**: 페이지 1-5 범위, 결과 위치 무작위화
**검증**: 285-307번 줄 fetchFromPexels 함수 검토
**영향범위**: lib/imageApi.ts

### 수정 5: Pixabay 배열 인덱싱 버그
**문제**: `[30, 50].random?.[Math.floor(...)]` 문법 오류
**원인**: 배열에 `.random` 속성이 없음
**해결**: `[30, 50][Math.floor(Math.random() * 2)]` 사용
**검증**: 231번 줄 수정 확인
**영향범위**: lib/imageApi.ts fetchFromPixabay 함수

### 수정 6: 썸네일 재생성 시 같은 이미지 반환
**문제**: "AI 생성" 버튼 클릭 시 같은 이미지만 반환
**원인**: handleRegenerateImage에서 기존 프롬프트를 그대로 사용
**해결**: 프롬프트에 `[v{random}]` 수정자 추가하여 매번 다른 결과 유도
**검증**: 270-284번 줄 수정 확인
**영향범위**: app/agent/images/page.tsx handleRegenerateImage 함수

---

## 회귀 테스트 (이전 문제 재발 확인)

- [ ] 크레딧 표시 (이전 "0/2" 에러) - 정상 작동
- [ ] 사용자 정보 오류 (이전 "사용자 정보가 없습니다") - 정상 작동
- [ ] 다중 선택 모달 - 정상 작동
- [ ] 페이지 새로 고침 시 이미지 삭제 - 정상 작동
